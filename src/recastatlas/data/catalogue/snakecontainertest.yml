name: testing/snakecontainertest

metadata:
  author: recast-atlas developers
  input requirements: ''
  short_description: Snakemake per-rule container test (requires apptainer SDM)

spec:
  workflow_type: snakemake
  workflow: snakecontainer/Snakefile

example_inputs:
  default:
    initdata:
      name: RECAST

results:
- name: container greeting
  relpath: results/result.yml
  load_yaml: true
